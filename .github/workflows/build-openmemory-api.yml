# nomerge
name: Build and Push OpenMemory API Image

on:
  push:
    branches:
      - main-new
    paths:
      - 'openmemory/api/**'
      - '.github/workflows/build-openmemory-api.yml'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build mem0 wheel
        run: |
          pip install build
          python -m build
          mkdir -p openmemory/api/wheels
          cp dist/mem0ai-*.whl openmemory/api/wheels/

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context: ./openmemory/api
          file: ./openmemory/api/Dockerfile.dev
          platforms: linux/amd64,linux/arm64
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          tags: |
            ghcr.io/${{ github.repository }}/openmemory-api:latest
            ghcr.io/${{ github.repository }}/openmemory-api:v${{ github.run_number }}.0.0
